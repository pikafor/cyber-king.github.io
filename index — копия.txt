<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes">
    <title>Берлога · компьютерный клуб</title>
    <!-- Шрифты (NEXUS + Берлога) -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;700&family=Unbounded:wght@600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        /* === ПОЛНОЕ ОФОРМЛЕНИЕ NEXUS (тёмное, геометричное, со спецэффектами) === */
        :root {
            --accent: #ff2d55;
            --accent2: #7b2dff;
            --accent3: #00e5ff;
            --dark: #030303;
            --card: #0a0a0a;
            --card-border: #1a1a1a;
            --text: #e0e0e0;
            --text-dim: #666;
            --glow-red: 0 0 40px rgba(255,45,85,0.3);
            --glow-purple: 0 0 40px rgba(123,45,255,0.3);
            --glow-cyan: 0 0 40px rgba(0,229,255,0.3);
            
            /* Дополнительные переменные для контента Берлоги (совместимы) */
            --bg-glass: rgba(20, 25, 35, 0.4);
            --bg-glass-dark: rgba(15, 18, 25, 0.7);
            --bg-glass-darker: rgba(10, 12, 18, 0.85);
            --glass-border: rgba(70, 130, 200, 0.25);
            --glass-border-light: rgba(100, 160, 230, 0.3);
            --accent-bright: #3f8ae0;
            --accent-dim: rgba(43, 122, 208, 0.15);
            --text-bright: #f2f6fc;
            --text-soft: #b8c7e0;
            --text-muted: #7f8fa3;
            --shadow-glass: 0 15px 35px -15px rgba(0, 0, 0, 0.9);
            --shadow-glass-hover: 0 20px 40px -15px rgba(0, 0, 0, 0.95);
            --transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; scroll-padding-top: 90px; scrollbar-width: thin; scrollbar-color: var(--accent) #111; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #111; }
        ::-webkit-scrollbar-thumb { background: var(--accent); border-radius: 3px; }
        body {
            background: var(--dark);
            color: var(--text-soft);
            font-family: 'Inter', sans-serif;
            overflow-x: hidden;
            min-height: 100vh;
            position: relative;
        }
        canvas#bg-canvas {
            display: block;
            position: fixed;
            top: 0; left: 0;
            width: 100vw; height: 100vh;
            z-index: 0;
        }
        /* Vanta фон поверх канваса? нет, мы оставляем только vanta, поэтому bg-canvas не используем, но оставим для совместимости, скроем */
        #bg-canvas { display: none; }
        
        /* Vanta фон */
        #vanta-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            opacity: 0.8;
            pointer-events: none;
        }
        .texture-overlay, .grain-overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: 1;
            pointer-events: none;
        }
        .texture-overlay {
            background: radial-gradient(circle at 30% 50%, rgba(0,0,0,0) 0%, rgba(0,0,0,0.3) 100%);
        }
        .grain-overlay {
            background-image: url('data:image/svg+xml;base64,');
            opacity: 0.15;
            mix-blend-mode: multiply;
        }
        
        .content-wrapper {
            position: relative;
            z-index: 10;
        }
        /* NAV (NEXUS стиль, адаптирован) */
        .nav {
            position: sticky;
            top: 0; left: 0; right: 0;
            z-index: 1000;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.4s ease;
            backdrop-filter: blur(0px);
        }
        .nav.scrolled {
            background: rgba(3,3,3,0.85);
            backdrop-filter: blur(20px);
            padding: 14px 50px;
            border-bottom: 1px solid rgba(255,45,85,0.1);
        }
        .nav-logo {
            font-size: 28px;
            font-weight: 900;
            letter-spacing: 6px;
            text-transform: uppercase;
            background: linear-gradient(135deg, var(--accent), var(--accent2));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .nav-logo img { width: 42px; height: 42px; filter: drop-shadow(0 0 10px var(--accent-dim)); }
        .nav-links { display: flex; gap: 35px; list-style: none; align-items: center; }
        .nav-links a {
            color: var(--text-dim);
            text-decoration: none;
            font-size: 13px;
            font-weight: 500;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: color 0.3s;
            position: relative;
        }
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -4px; left: 0;
            width: 0; height: 2px;
            background: var(--accent);
            transition: width 0.3s ease;
        }
        .nav-links a:hover { color: var(--accent); }
        .nav-links a:hover::after { width: 100%; }
        .nav-social { display: flex; gap: 12px; margin-left: 20px; align-items: center; }
        .social-link { color: var(--text-muted) !important; font-size: 1.2rem !important; padding: 0 !important; border-bottom: none !important; transition: color var(--transition) !important; }
        .social-link:hover { color: var(--accent) !important; border-bottom: none !important; }
        .cta-button {
            background: transparent;
            border: 1px solid var(--accent);
            color: var(--accent) !important;
            padding: 8px 24px !important;
            font-weight: 600 !important;
            margin-left: 16px;
            transition: all var(--transition) !important;
            border-bottom: 1px solid var(--accent) !important;
            text-decoration: none !important;
        }
        .cta-button:hover { background: var(--accent); color: var(--bg-deep) !important; border-color: var(--accent); }
        .hamburger {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            z-index: 1001;
        }
        .hamburger span { width: 25px; height: 2px; background: var(--text); transition: all 0.3s; }
        .hamburger.active span:nth-child(1) { transform: rotate(45deg) translate(5px,5px); }
        .hamburger.active span:nth-child(2) { opacity: 0; }
        .hamburger.active span:nth-child(3) { transform: rotate(-45deg) translate(5px,-5px); }
        .mobile-menu {
            position: fixed;
            inset: 0;
            background: rgba(3,3,3,0.97);
            z-index: 999;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 30px;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s;
        }
        .mobile-menu.active { opacity: 1; pointer-events: all; }
        .mobile-menu a { font-size: 24px; font-weight: 700; color: var(--text); text-decoration: none; transition: color 0.3s; }
        .mobile-menu a:hover { color: var(--accent); }
        
        /* HERO (Берлога стиль + NEXUS анимации) */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 30px;
            position: relative;
        }
        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 20px;
            border: 1px solid rgba(255,45,85,0.3);
            border-radius: 30px;
            font-size: 12px;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--accent);
            margin-bottom: 30px;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeUp 0.8s ease 0.5s forwards;
            background: var(--bg-glass);
            backdrop-filter: blur(8px);
        }
        .hero-badge .dot {
            width: 6px; height: 6px;
            background: var(--accent);
            border-radius: 50%;
            animation: pulse-dot 2s infinite;
        }
        @keyframes pulse-dot { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:0.4;transform:scale(0.6)} }
        .hero h1 {
            font-size: clamp(48px, 8vw, 120px);
            font-weight: 900;
            line-height: 1.05;
            letter-spacing: -2px;
            margin-bottom: 25px;
            opacity: 0;
            transform: translateY(40px);
            animation: fadeUp 1s ease 0.8s forwards;
            font-family: 'Unbounded', sans-serif;
            text-transform: uppercase;
            text-shadow: 0 10px 30px rgba(0,0,0,0.8);
            animation: glitch 5s infinite, fadeUp 1s ease 0.8s forwards;
        }
        @keyframes glitch { 0%,100%{transform:skew(0deg,0deg);text-shadow:0 10px 30px rgba(0,0,0,0.8)} 96%{transform:skew(2deg,0.5deg);text-shadow:-2px 0 var(--accent),2px 0 var(--accent2)} 97%{transform:skew(-1deg,-0.2deg)} 98%{transform:skew(0deg,0deg)} }
        .hero h1 .gradient-text {
            background: linear-gradient(135deg, var(--accent), var(--accent2), var(--accent3));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-size: 300% 300%;
            animation: gradientShift 5s ease infinite;
        }
        @keyframes gradientShift { 0%{background-position:0% 50%} 50%{background-position:100% 50%} 100%{background-position:0% 50%} }
        .hero-sub {
            font-size: 18px;
            color: var(--text-soft);
            max-width: 600px;
            line-height: 1.7;
            margin-bottom: 45px;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeUp 1s ease 1.1s forwards;
        }
        .hero-buttons {
            display: flex;
            gap: 20px;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeUp 1s ease 1.4s forwards;
        }
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 16px 40px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: 1px solid transparent;
            cursor: pointer;
        }
        .btn-primary {
            background: linear-gradient(135deg, var(--accent), var(--accent2));
            color: #fff;
        }
        .btn-primary:hover { transform: translateY(-3px); box-shadow: var(--glow-red); }
        .btn-outline {
            background: transparent;
            color: var(--text);
            border: 1px solid var(--card-border);
        }
        .btn-outline:hover { border-color: var(--accent); color: var(--accent); transform: translateY(-3px); }
        .btn .btn-ripple {
            position: absolute; border-radius: 50%; background: rgba(255,255,255,0.3); transform: scale(0); animation: ripple 0.6s ease-out; pointer-events: none;
        }
        @keyframes ripple { to { transform: scale(4); opacity: 0; } }
        .hero-scroll {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
            margin-top: 50px;          /* отступ от кнопок */
            opacity: 0;
            transform: translateY(20px);
            animation: fadeUp 1s ease 1.8s forwards;
            position: relative;        /* вместо absolute */
            bottom: auto;
            left: auto;
            right: auto;
        }
        .hero-scroll span { font-size: 11px; letter-spacing: 3px; text-transform: uppercase; color: var(--text-dim); }
        .scroll-indicator { width: 24px; height: 40px; border: 2px solid var(--text-dim); border-radius: 12px; position: relative; }
        .scroll-indicator::before { content: ''; width: 4px; height: 8px; background: var(--accent); border-radius: 2px; position: absolute; top: 6px; left: 50%; transform: translateX(-50%); animation: scrollBounce 2s infinite; }
        @keyframes scrollBounce { 0%,100%{top:6px;opacity:1} 50%{top:22px;opacity:0.3} }
        @keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }
        
        .hero-stats {
            display: flex;
            gap: 60px;
            margin: 50px 0 60px;
            flex-wrap: wrap;
            justify-content: center;
        }
        .stat-item {
            text-align: center;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeUp 0.8s ease-out forwards;
        }
        .stat-item:nth-child(1) { animation-delay: 0.1s; }
        .stat-item:nth-child(2) { animation-delay: 0.2s; }
        .stat-item:nth-child(3) { animation-delay: 0.3s; }
        .stat-num {
            font-size: 2.8rem;
            font-weight: 700;
            font-family: 'Unbounded', sans-serif;
            color: var(--text-bright);
            display: block;
            line-height: 1.2;
            margin-bottom: 5px;
        }
        .stat-label { color: var(--text-muted); font-size: 0.85rem; text-transform: uppercase; letter-spacing: 1.5px; }
        
        /* Секции */
        section {
            padding: 120px 50px;
            position: relative;
        }
        .section-header {
            text-align: center;
            margin-bottom: 80px;
        }
        .section-label {
            display: inline-block;
            font-size: 12px;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: var(--accent);
            margin-bottom: 15px;
            font-weight: 600;
        }
        h2 {
            font-size: clamp(1.7rem, 5vw, 3.5rem);
            margin-bottom: 0.8em;
            position: relative;
            display: inline-block;
            padding-left: 30px;
            border-left: 4px solid var(--accent);
            animation: slideIn 0.8s ease-out;
            font-family: 'Unbounded', sans-serif;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: -0.02em;
            color: var(--text-bright);
        }
        @keyframes slideIn { from { opacity: 0; transform: translateX(-30px); } to { opacity: 1; transform: translateX(0); } }
        .subhead {
            font-size: 1.2rem;
            color: var(--text-soft);
            max-width: 720px;
            margin-bottom: 3rem;
            font-weight: 300;
            line-height: 1.5;
            animation: fadeUp 0.8s ease-out;
        }
        
        /* Стеклянные карточки (адаптация NEXUS feature-card под Берлогу) */
        .grid-3 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin: 50px 0;
        }
        .glass-card {
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 16px;
            padding: 40px 35px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(40px);
            box-shadow: var(--shadow-glass);
        }
        .glass-card.visible { opacity: 1; transform: translateY(0); }
        .glass-card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, var(--accent), transparent);
            opacity: 0;
            transition: opacity 0.4s;
        }
        .glass-card:hover::before { opacity: 1; }
        .glass-card:hover {
            border-color: rgba(255,45,85,0.2);
            transform: translateY(-8px);
            box-shadow: 0 20px 60px rgba(0,0,0,0.5);
        }
        .glass-card-title {
            font-family: 'Unbounded', sans-serif;
            font-weight: 700;
            font-size: 1.8rem;
            color: var(--text-bright);
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--card-border);
            transition: border-color var(--transition);
        }
        .glass-card:hover .glass-card-title { border-bottom-color: rgba(255,45,85,0.2); }
        .feature-list { list-style: none; }
        .feature-list li {
            margin: 15px 0;
            display: flex;
            align-items: center;
            gap: 15px;
            color: var(--text-soft);
            transition: transform var(--transition);
        }
        .feature-list li:hover { transform: translateX(8px); color: var(--text-bright); }
        .feature-list i { color: var(--accent); font-size: 1.2rem; width: 24px; }
        .advantage-icon { font-size: 2.2rem; color: var(--accent); margin-bottom: 20px; transition: transform var(--transition); }
        .glass-card:hover .advantage-icon { transform: scale(1.05); }
        
        /* Галерея (NEXUS gallery) */
        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin: 50px 0;
        }
        .gallery-item {
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 14px;
            overflow: hidden;
            position: relative;
            cursor: pointer;
            transition: all 0.4s;
            aspect-ratio: 4/3;
        }
        .gallery-item:hover {
            transform: scale(1.03);
            z-index: 2;
            box-shadow: 0 20px 50px rgba(0,0,0,0.6);
            border-color: var(--accent);
        }
        .gallery-item img { width: 100%; height: 100%; object-fit: cover; display: block; filter: grayscale(0.3) brightness(0.8); transition: transform 0.5s ease; }
        .gallery-item:hover img { transform: scale(1.08); filter: grayscale(0) brightness(0.9); }
        .gallery-caption {
            position: absolute;
            bottom: 0; left: 0; right: 0;
            background: linear-gradient(to top, rgba(0,0,0,0.9), transparent);
            color: var(--text-bright);
            padding: 30px 15px 15px;
            font-weight: 500;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            transform: translateY(100%);
            transition: transform 0.4s ease;
            backdrop-filter: blur(4px);
        }
        .gallery-item:hover .gallery-caption { transform: translateY(0); }
        
        /* Тарифы (слайдер) */
        .tariffs-wrapper { position: relative; display: flex; align-items: center; margin: 40px 0; }
        .tariffs-slider { overflow: hidden; width: 100%; padding: 15px 0; }
        .tariffs-track { display: flex; gap: 24px; transition: transform 0.5s ease; will-change: transform; }
        .tariff-card {
            flex: 0 0 calc((100% - (24px * 2)) / 3);
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 18px;
            padding: 35px;
            transition: all 0.4s;
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(40px);
            box-shadow: var(--shadow-glass);
        }
        .tariff-card.visible { opacity: 1; transform: translateY(0); }
        .tariff-card:hover {
            transform: translateY(-10px);
            border-color: rgba(255,45,85,0.2);
            box-shadow: 0 25px 60px rgba(0,0,0,0.5);
        }
        .tariff-name {
            font-family: 'Unbounded', sans-serif;
            font-weight: 700;
            font-size: 1.6rem;
            color: var(--text-bright);
            margin-bottom: 20px;
            padding-bottom: 12px;
            border-bottom: 1px solid var(--card-border);
        }
        .tariff-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px dashed var(--card-border);
            color: var(--text-soft);
            transition: all var(--transition);
        }
        .tariff-card:hover .tariff-row { border-bottom-color: rgba(255,45,85,0.15); padding-left: 5px; }
        .tariff-price { color: var(--accent-bright); font-weight: 600; font-size: 1.2rem; }
        .night-row { background: rgba(255,45,85,0.05); border: none; padding: 12px 16px; margin-top: 12px; }
        .slider-btn {
            background: var(--card);
            backdrop-filter: blur(8px);
            border: 1px solid var(--card-border);
            color: var(--text-soft);
            width: 44px;
            height: 44px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.1rem;
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            transition: all var(--transition);
            z-index: 20;
            border-radius: 8px;
        }
        .slider-btn:hover { border-color: var(--accent); color: var(--accent); background: var(--card); transform: translateY(-50%) scale(1.1); }
        .prev-btn { left: -20px; }
        .next-btn { right: -20px; }
        
        /* Игры */
        .games-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            padding: 30px 0;
        }
        .game-item {
            position: relative;
            height: 200px;
            overflow: hidden;
            border: 1px solid var(--card-border);
            border-radius: 14px;
            transition: all 0.4s;
        }
        .game-item img { width: 100%; height: 100%; object-fit: cover; filter: brightness(0.7); transition: all 0.5s ease; }
        .game-item:hover { border-color: var(--accent); transform: scale(1.02); }
        .game-item:hover img { filter: brightness(0.9); transform: scale(1.08); }
        .game-item span {
            position: absolute; bottom: 15px; left: 15px;
            color: var(--text-bright); font-weight: 600; font-size: 1.1rem;
            text-shadow: 0 2px 5px rgba(0,0,0,0.5); z-index: 2;
            font-family: 'Unbounded', sans-serif; letter-spacing: 0.5px;
        }
        
        /* FAQ */
        .faq-section { margin: 60px 0; }
        .faq-item {
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 12px;
            margin-bottom: 12px;
            overflow: hidden;
            transition: all 0.3s;
        }
        .faq-question {
            padding: 22px 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            cursor: pointer;
            font-weight: 600;
            font-size: 15px;
            color: var(--text-bright);
            user-select: none;
        }
        .faq-question i { transition: transform var(--transition); color: var(--accent); }
        .faq-item.active .faq-question i { transform: rotate(45deg); }
        .faq-answer {
            max-height: 0;
            padding: 0 25px;
            overflow: hidden;
            transition: max-height 0.4s ease, padding 0.4s ease;
            color: var(--text-soft);
            border-top: 0px solid var(--card-border);
        }
        .faq-item.active .faq-answer { max-height: 200px; padding: 0 25px 22px; border-top-width: 1px; }
        
        /* Бронь */
        .booking-section {
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 18px;
            padding: 60px;
            margin: 60px 0;
            box-shadow: var(--shadow-glass);
        }
        .booking-section h2 { border-left: none; text-align: center; padding-left: 0; display: block; }
        .booking-form { max-width: 600px; margin: 40px auto 0; display: flex; flex-direction: column; gap: 20px; }
        .input-group {
            display: flex;
            align-items: center;
            background: rgba(0,0,0,0.3);
            border: 1px solid var(--card-border);
            padding: 4px 4px 4px 20px;
            transition: all var(--transition);
            border-radius: 8px;
        }
        .input-group:focus-within { border-color: var(--accent); box-shadow: 0 0 0 2px rgba(255,45,85,0.1); }
        .input-group span { color: var(--accent); font-weight: 500; font-size: 0.95rem; padding-right: 12px; border-right: 1px solid var(--card-border); }
        .input-group input { background: transparent; border: none; padding: 15px 20px; width: 100%; color: var(--text-bright); font-size: 1rem; outline: none; }
        
        /* Карта */
        .map {
            height: 400px;
            margin: 50px 0;
            overflow: hidden;
            transition: all var(--transition);
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 16px;
            box-shadow: var(--shadow-glass);
        }
        .map iframe { width: 100%; height: 100%; filter: grayscale(0.8) contrast(1.1); transition: filter 0.4s; display: block; }
        .map:hover { border-color: rgba(255,45,85,0.2); transform: translateY(-3px); }
        .map:hover iframe { filter: grayscale(0.4) contrast(1.1); }
        
        /* Футер */
        .footer {
            padding: 80px 50px 30px;
            border-top: 1px solid var(--card-border);
            background: var(--bg-glass-darker);
            backdrop-filter: blur(12px);
        }
        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 50px;
            max-width: 1200px;
            margin: 0 auto 60px;
        }
        .footer-logo {
            font-size: 2rem;
            font-weight: 700;
            font-family: 'Unbounded', sans-serif;
            color: var(--text-bright);
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        .footer-logo img { width: 42px; height: 42px; }
        .footer-info p { margin: 10px 0; color: var(--text-soft); transition: transform var(--transition); }
        .footer-info p:hover { transform: translateX(5px); color: var(--accent); }
        .footer-info i { color: var(--accent); width: 25px; margin-right: 10px; }
        .social-links a { color: var(--text-soft); font-size: 1.8rem; margin-right: 20px; transition: all var(--transition); display: inline-block; }
        .social-links a:hover { color: var(--accent); transform: translateY(-3px); }
        .footer-bottom {
            max-width: 1200px;
            margin: 0 auto;
            padding-top: 30px;
            border-top: 1px solid var(--card-border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 13px;
            color: var(--text-dim);
        }
        
        /* Back to top */
        .back-to-top {
            position: fixed;
            bottom: 30px; right: 30px;
            width: 48px; height: 48px;
            border-radius: 12px;
            background: var(--card);
            border: 1px solid var(--card-border);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: var(--text);
            cursor: pointer;
            z-index: 999;
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.3s;
        }
        .back-to-top.visible { opacity: 1; transform: translateY(0); }
        .back-to-top:hover { border-color: var(--accent); color: var(--accent); transform: translateY(-3px); }
        
        /* Адаптив */
        @media (max-width: 1100px) {
            .nav-links { display: none; }
            .hamburger { display: flex; }
            .nav-links.mobile-open { display: flex; flex-direction: column; position: absolute; top: 70px; left: 0; width: 100%; background: var(--bg-glass-darker); backdrop-filter: blur(12px); padding: 30px; gap: 15px; border-bottom: 1px solid var(--card-border); }
            .nav-links.mobile-open .nav-social { display: flex; margin-left: 0; margin-top: 10px; }
        }
        @media (max-width: 1024px) {
            .prev-btn { left: -10px; }
            .next-btn { right: -10px; }
            .tariff-card { flex: 0 0 calc((100% - 24px) / 2); }
        }
        @media (max-width: 768px) {
            section { padding: 80px 25px; }
            .nav { padding: 15px 25px; }
            .container { padding: 0 20px; }
            .gallery-grid { grid-template-columns: repeat(2, 1fr); }
            .games-grid { grid-template-columns: repeat(2, 1fr); }
            .tariff-card { flex: 0 0 100%; }
            .booking-section { padding: 40px 20px; }
            .map { height: 300px; }
            .hero-stats { gap: 30px; }
            .stat-num { font-size: 2.2rem; }
            .footer-grid { grid-template-columns: 1fr 1fr; }
            .hero-buttons { flex-direction: column; }
        }
        @media (max-width: 600px) {
            .gallery-grid { grid-template-columns: 1fr; }
            .games-grid { grid-template-columns: 1fr; }
            .btn { width: 100%; justify-content: center; }
            .subhead { font-size: 1rem; }
            .glass-card { padding: 25px 20px; }
            .footer-grid { grid-template-columns: 1fr; }
        }
        @media (max-width: 480px) {
            h2 { padding-left: 20px; }
            .hero-badge { font-size: 0.75rem; padding: 4px 16px; }
        }
        /* Фикс для центрирования заголовка в брони */
        .booking-section h2 { display: block !important; width: 100%; text-align: center; border-left: none; padding-left: 0; }
        /* Мобильный скролл тарифов */
        @media (max-width: 768px) {
            .tariffs-wrapper { margin: 40px 0; }
            .tariffs-slider { overflow-x: auto; -webkit-overflow-scrolling: touch; scroll-snap-type: x mandatory; padding: 15px 5px 25px; margin: 0 -20px; padding-left: 20px; padding-right: 20px; scrollbar-width: thin; scrollbar-color: var(--accent) var(--card); }
            .tariffs-slider::-webkit-scrollbar { height: 4px; }
            .tariffs-slider::-webkit-scrollbar-track { background: var(--card); }
            .tariffs-slider::-webkit-scrollbar-thumb { background: var(--accent); }
            .tariffs-track { width: max-content; transform: none !important; transition: none; }
            .tariff-card { flex: 0 0 280px; scroll-snap-align: start; }
            .slider-btn { display: none; }
        }
        /* Подсказка "листайте" – по умолчанию скрыта */
        .swipe-hint {
            display: none;
            align-items: center;
            justify-content: center;
            gap: 12px;
            margin-top: 15px;
            font-size: 13px;
            color: var(--text-muted);
            letter-spacing: 1px;
        }

        .swipe-hint i {
            font-size: 14px;
            animation: swipeBounce 1.4s infinite ease-in-out;
        }

        @keyframes swipeBounce {
            0% { transform: translateX(0); opacity: 0.5; }
            50% { transform: translateX(5px); opacity: 1; }
            100% { transform: translateX(0); opacity: 0.5; }
        }

        /* На мобильных показываем подсказку и добавляем маску-намёк, что справа есть ещё карточки */
        @media (max-width: 768px) {
            .swipe-hint {
                display: flex;
            }

            /* Градиентная маска: правый край становится прозрачным – видно, что можно листать */
            .tariffs-slider {
                mask-image: linear-gradient(to right, black 85%, transparent 98%);
                -webkit-mask-image: linear-gradient(to right, black 85%, transparent 98%);
            }

            /* Небольшая тень в конце контейнера для усиления эффекта (опционально) */
            .tariffs-wrapper {
                position: relative;
            }
            .tariffs-wrapper::after {
                content: '';
                position: absolute;
                right: 0;
                top: 0;
                bottom: 0;
                width: 40px;
                background: linear-gradient(to left, rgba(0,0,0,0.6), transparent);
                pointer-events: none;
                z-index: 5;
                border-radius: 0 16px 16px 0;
            }
        }
    </style>
</head>
<body>
    <!-- Vanta фон -->
    <div id="vanta-bg"></div>
    <div class="texture-overlay"></div>
    <div class="grain-overlay"></div>

    <div class="content-wrapper">
        <!-- НАВИГАЦИЯ (микс) -->
        <nav class="nav" id="nav">
            <a href="#" class="nav-logo">
                <img src="https://static.tildacdn.com/tild3735-6135-4337-a666-613139383237/Group_1321315100.svg" alt="Берлога" style="width:42px; height:42px;">
                БЕРЛОГА
            </a>
            <ul class="nav-links" id="navLinks">
                <li><a href="#computers">КОМПЫ</a></li>
                <li><a href="#tariffs">ТАРИФЫ</a></li>
                <li><a href="#games">ИГРЫ</a></li>
                <li><a href="#booking">БРОНЬ</a></li>
                <li><a href="#map">КАРТА</a></li>
                <li><a href="#contacts">КОНТАКТЫ</a></li>
                <div class="nav-social">
                    <a href="https://vk.com/berloga_gaming61" class="social-link"><i class="fab fa-vk"></i></a>
                    <a href="https://t.me/Berloga_gaming" class="social-link"><i class="fab fa-telegram"></i></a>
                    <a href="https://wa.me/+79889980909" class="social-link"><i class="fab fa-whatsapp"></i></a>
                </div>
                <li><a href="#booking" class="cta-button">ЗАБРОНИРОВАТЬ</a></li>
            </ul>
            <div class="hamburger" id="hamburger">
                <span></span><span></span><span></span>
            </div>
        </nav>
        <div class="mobile-menu" id="mobileMenu">
            <a href="#computers" onclick="closeMobile()">КОМПЫ</a>
            <a href="#tariffs" onclick="closeMobile()">ТАРИФЫ</a>
            <a href="#games" onclick="closeMobile()">ИГРЫ</a>
            <a href="#booking" onclick="closeMobile()">БРОНЬ</a>
            <a href="#map" onclick="closeMobile()">КАРТА</a>
            <a href="#contacts" onclick="closeMobile()">КОНТАКТЫ</a>
        </div>

        <!-- HERO -->
        <section class="hero" id="hero">
            <div class="hero-badge"><span class="dot"></span> УЛ. МЕЧНИКОВА · КРУГЛОСУТОЧНО</div>
            <h1>БЕРЛОГА</h1>
            <p class="hero-sub">RTX 4060/4070 · 240 Гц · Дикое железо в центре</p>
            <div class="hero-stats">
                <div class="stat-item"><span class="stat-num">240</span> <span class="stat-label">Герц</span></div>
                <div class="stat-item"><span class="stat-num">RTX</span> <span class="stat-label">40 серия</span></div>
                <div class="stat-item"><span class="stat-num">24/7</span> <span class="stat-label">без сна</span></div>
            </div>
            <div class="hero-buttons">
                <a href="#booking" class="btn btn-primary">Забрать место <i class="fas fa-arrow-right"></i></a>
                <a href="#computers" class="btn btn-outline">Конфигурации</a>
            </div>
            <div class="hero-scroll">
                <div class="scroll-indicator"></div>
                <span>Прокрутите вниз</span>
            </div>
        </section>

        <!-- КОМПЬЮТЕРЫ -->
        <section id="computers">
            <h2>Конфигурации</h2>
            <p class="subhead">Железо, которое не тормозит</p>
            <div class="grid-3">
                <div class="glass-card">
                    <div class="glass-card-title">СТАНДАРТ</div>
                    <ul class="feature-list">
                        <li><i class="fas fa-desktop"></i> 23.8" AOC 165 Гц</li>
                        <li><i class="fas fa-microchip"></i> RTX 4060 / i5-12400F</li>
                        <li><i class="fas fa-memory"></i> 16GB DDR4</li>
                        <li><i class="fas fa-headphones"></i> HyperX Cloud II</li>
                    </ul>
                </div>
                <div class="glass-card">
                    <div class="glass-card-title">БУТКЕМП</div>
                    <ul class="feature-list">
                        <li><i class="fas fa-desktop"></i> 24.5" AOC 240 Гц</li>
                        <li><i class="fas fa-microchip"></i> RTX 4060 Ti / i5-13400F</li>
                        <li><i class="fas fa-memory"></i> 32GB DDR4</li>
                        <li><i class="fas fa-headphones"></i> HyperX Cloud II</li>
                    </ul>
                </div>
                <div class="glass-card">
                    <div class="glass-card-title">СУПЕРБУН</div>
                    <ul class="feature-list">
                        <li><i class="fas fa-desktop"></i> 27" 280 Гц</li>
                        <li><i class="fas fa-microchip"></i> RTX 4070 / i7-13700F</li>
                        <li><i class="fas fa-memory"></i> 32GB DDR5</li>
                        <li><i class="fas fa-headphones"></i> Audeze Maxwell</li>
                    </ul>
                </div>
            </div>
        </section>

        <!-- ПРЕИМУЩЕСТВА -->
        <section id="advantages">
            <h2>Почему мы</h2>
            <p class="subhead">Комфорт и внимание к деталям</p>
            <div class="grid-3">
                <div class="glass-card">
                    <div class="advantage-icon"><i class="fas fa-wind"></i></div>
                    <div class="glass-card-title" style="font-size: 1.5rem;">ЧИСТОТА</div>
                    <div>Ежедневная уборка и обработка периферии</div>
                </div>
                <div class="glass-card">
                    <div class="advantage-icon"><i class="fas fa-crown"></i></div>
                    <div class="glass-card-title" style="font-size: 1.5rem;">КОМФОРТ</div>
                    <div>Игровые кресла с поддержкой спины</div>
                </div>
                <div class="glass-card">
                    <div class="advantage-icon"><i class="fas fa-utensils"></i></div>
                    <div class="glass-card-title" style="font-size: 1.5rem;">СНЭК-ЗОНА</div>
                    <div>Кофе, чай, снеки без отрыва от игры</div>
                </div>
            </div>
        </section>

        <!-- ГАЛЕРЕЯ -->
        <section id="gallery">
            <h2>Атмосфера</h2>
            <p class="subhead">Как выглядят наши залы</p>
            <div class="gallery-grid">
                <div class="gallery-item">
                    <img src="https://optim.tildacdn.com/tild3166-3833-4036-b231-613337323037/-/format/webp/IMG_3438.jpg.webp" alt="Игровой зал">
                    <div class="gallery-caption">Основной зал</div>
                </div>
                <div class="gallery-item">
                    <img src="https://optim.tildacdn.com/tild3931-3937-4337-a261-653833313564/-/format/webp/IMG_3440.jpg.webp" alt="Компьютеры">
                    <div class="gallery-caption">Игровые места</div>
                </div>
                <div class="gallery-item">
                    <img src="https://optim.tildacdn.com/tild6164-6463-4130-a631-643230663339/-/format/webp/IMG_3444.jpg.webp" alt="Турнир">
                    <div class="gallery-caption">ВИП зона</div>
                </div>
            </div>
        </section>

        <!-- ТАРИФЫ -->
        <section id="tariffs">
            <h2>Тарифы</h2>
            <p class="subhead">Дневные и ночные пакеты</p>
            <div class="tariffs-wrapper">
                <button class="slider-btn prev-btn" id="prevTariffBtn"><i class="fas fa-chevron-left"></i></button>
                <div class="tariffs-slider">
                    <div class="tariffs-track" id="tariffsTrack">
                        <div class="tariff-card">
                            <div class="tariff-name">Комфорт</div>
                            <div class="tariff-row"><span>1 час</span><span class="tariff-price">120₽</span></div>
                            <div class="tariff-row"><span>3 часа</span><span class="tariff-price">300₽</span></div>
                            <div class="tariff-row"><span>6 часов</span><span class="tariff-price">550₽</span></div>
                            <div class="tariff-row"><span>9 часов</span><span class="tariff-price">750₽</span></div>
                            <div class="tariff-row night-row"><span>☀️ Утренний (08:00–13:00)</span><span class="tariff-price">250₽</span></div>
                            <div class="tariff-row night-row"><span>🌙 Ночной (22:00–08:00)</span><span class="tariff-price">500₽</span></div>
                        </div>
                        <div class="tariff-card">
                            <div class="tariff-name">Буткемп</div>
                            <div class="tariff-row"><span>1 час</span><span class="tariff-price">150₽</span></div>
                            <div class="tariff-row"><span>3 часа</span><span class="tariff-price">400₽</span></div>
                            <div class="tariff-row"><span>6 часов</span><span class="tariff-price">700₽</span></div>
                            <div class="tariff-row"><span>9 часов</span><span class="tariff-price">990₽</span></div>
                            <div class="tariff-row night-row"><span>☀️ Утренний (08:00–13:00)</span><span class="tariff-price">350₽</span></div>
                            <div class="tariff-row night-row"><span>🌙 Ночной (22:00–08:00)</span><span class="tariff-price">850₽</span></div>
                        </div>
                        <div class="tariff-card">
                            <div class="tariff-name">VIP</div>
                            <div class="tariff-row"><span>1 час</span><span class="tariff-price">220₽</span></div>
                            <div class="tariff-row"><span>3 часа</span><span class="tariff-price">600₽</span></div>
                            <div class="tariff-row"><span>6 часов</span><span class="tariff-price">1100₽</span></div>
                            <div class="tariff-row"><span>9 часов</span><span class="tariff-price">1500₽</span></div>
                            <div class="tariff-row night-row"><span>☀️ Утренний (08:00–13:00)</span><span class="tariff-price">450₽</span></div>
                            <div class="tariff-row night-row"><span>🌙 Ночной (22:00–08:00)</span><span class="tariff-price">1050₽</span></div>
                        </div>
                        <div class="tariff-card">
                            <div class="tariff-name">Двойка</div>
                            <div class="tariff-row"><span>1 час</span><span class="tariff-price">230₽</span></div>
                            <div class="tariff-row"><span>3 часа</span><span class="tariff-price">650₽</span></div>
                            <div class="tariff-row"><span>6 часов</span><span class="tariff-price">1200₽</span></div>
                            <div class="tariff-row"><span>9 часов</span><span class="tariff-price">1650₽</span></div>
                            <div class="tariff-row night-row"><span>☀️ Утренний (08:00–13:00)</span><span class="tariff-price">500₽</span></div>
                            <div class="tariff-row night-row"><span>🌙 Ночной (22:00–08:00)</span><span class="tariff-price">1100₽</span></div>
                        </div>
                        <div class="tariff-card">
                            <div class="tariff-name">Премиум тв</div>
                            <div class="tariff-row"><span>1 час</span><span class="tariff-price">350₽</span></div>
                            <div class="tariff-row"><span>Кино</span><span class="tariff-price">450₽</span></div>
                            <div class="tariff-row"><span>6 часов</span><span class="tariff-price">1200₽</span></div>
                            <div class="tariff-row night-row"><span>☀️ Утренний (08:00–13:00)</span><span class="tariff-price">500₽</span></div>
                            <div class="tariff-row night-row"><span>🌙 Ночной (22:00–08:00)</span><span class="tariff-price">1250₽</span></div>
                        </div>
                    </div>
                </div>
                <button class="slider-btn next-btn" id="nextTariffBtn"><i class="fas fa-chevron-right"></i></button>
            </div>
            <div class="swipe-hint">
                <i class="fas fa-chevron-left"></i> листайте <i class="fas fa-chevron-right"></i>
            </div>
        </section>

        <!-- ИГРЫ -->
        <section id="games">
            <h2>Игры</h2>
            <p class="subhead">Лучшие игры</p>
            <div class="games-grid">
                <div class="game-item"><img src="dota.png" alt="Dota 2"><span>DOTA 2</span></div>
                <div class="game-item"><img src="cs.png" alt="CS 2"><span>CS 2</span></div>
                <div class="game-item"><img src="fortnite.png" alt="Fortnite"><span>FORTNITE</span></div>
                <div class="game-item"><img src="minecraft.png" alt="Minecraft"><span>MINECRAFT</span></div>
                <div class="game-item"><img src="lol.png" alt="Valorant"><span>LEAGUE OF LEGENDS</span></div>
                <div class="game-item"><img src="valorant.png" alt="Valorant"><span>VALORANT</span></div>
                <div class="game-item"><img src="deadlock.png" alt="Valorant"><span>DEADLOCK</span></div>
                <div class="game-item"><img src="apex.png" alt="Apex"><span>APEX</span></div>
                <div class="game-item"><img src="cod.png" alt="Apex"><span>COD MODERN WARFARE |||</span></div>
            </div>
        </section>

        <!-- FAQ -->
        <section id="faq" class="faq-section">
            <h2>Вопросы</h2>
            <p class="subhead">Ответы на частые вопросы</p>
            <div class="faq-item">
                <div class="faq-question">Скидки для студентов? <i class="fas fa-plus"></i></div>
                <div class="faq-answer">Да, 10% в будни с 10:00 до 18:00 по студенческому.</div>
            </div>
            <div class="faq-item">
                <div class="faq-question">Можно со своей едой? <i class="fas fa-plus"></i></div>
                <div class="faq-answer">Можно, но у нас есть снэк-зона с ценами ниже средних.</div>
            </div>
            <div class="faq-item">
                <div class="faq-question">Бронь для команды? <i class="fas fa-plus"></i></div>
                <div class="faq-answer">Оставьте заявку ниже. Для групп от 5 человек скидка 15%.</div>
            </div>
            <div class="faq-item">
                <div class="faq-question">Работаете ночью? <i class="fas fa-plus"></i></div>
                <div class="faq-answer">24/7, без выходных. Приходите в любое время.</div>
            </div>
        </section>

        <!-- БРОНЬ -->
        <section id="booking">
            <div class="booking-section">
                <h2>Забронировать</h2>
                <p style="text-align: center; color: var(--text-soft);">Оставьте заявку — мы перезвоним через 5 минут</p>
                <form class="booking-form" id="bookingForm" onsubmit="event.preventDefault(); alert('Заявка отправлена!');">
                    <div class="input-group"><span>Имя</span><input type="text" placeholder="Медведь" required></div>
                    <div class="input-group"><span>+7</span><input type="tel" placeholder="(000) 000-00-00" required></div>
                    <div class="input-group"><span>ПК</span><input type="text" placeholder="Стандарт / Буткемп / VIP"></div>
                    <button type="submit" class="btn btn-primary" style="justify-content: center;">Забронировать <i class="fas fa-paw"></i></button>
                </form>
            </div>
        </section>

        <!-- КАРТА -->
        <section id="map">
            <h2>Где нас искать</h2>
            <p class="subhead">ул. Мечникова, 31/106</p>
            <div class="map">
                <iframe src="https://yandex.ru/map-widget/v1/?lang=ru_RU&scroll=true&source=constructor-api&um=constructor%3A2c9224ddcfa49014c9335334c320f10ac69767347fde82b34938be5374693fb5" allowfullscreen="true" loading="lazy"></iframe>
            </div>
        </section>

        <!-- КОНТАКТЫ -->
        <section id="contacts">
            <h2>Контакты</h2>
            <div class="footer-grid">
                <div>
                    <div class="footer-logo">
                        <img src="https://static.tildacdn.com/tild3735-6135-4337-a666-613139383237/Group_1321315100.svg" alt="Берлога" class="footer-logo-img">
                        БЕРЛОГА
                    </div>
                    <p>Компьютерный клуб</p>
                </div>
                <div class="footer-info">
                    <p><i class="fas fa-phone-alt"></i>+7 988 998 09 09</p>
                    <p><i class="fas fa-clock"></i> 24/7</p>
                </div>
                <div class="social-links">
                    <a href="https://vk.com/berloga_gaming61"><i class="fab fa-vk"></i></a>
                    <a href="https://t.me/Berloga_gaming"><i class="fab fa-telegram"></i></a>
                    <a href="https://wa.me/+79889980909"><i class="fab fa-whatsapp"></i></a>
                </div>
            </div>
        </section>

        <!-- FOOTER -->
        <footer class="footer">
            <div class="footer-bottom">
                <span>© 2025 Берлога. Все права защищены</span>
            </div>
        </footer>
    </div>

    <!-- Кнопка наверх -->
    <button class="back-to-top" id="backToTop" onclick="window.scrollTo({top:0,behavior:'smooth'})">↑</button>

    <!-- Скрипты -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r134/three.min.js"></script>
    <script src="https://www.vantajs.com/dist/vanta.net.min.js"></script>
    <script>
    (function() {
        // Vanta NET фон
        VANTA.NET({
            el: "#vanta-bg",
            mouseControls: true,
            touchControls: true,
            gyroControls: false,
            minHeight: 200.00,
            minWidth: 200.00,
            scale: 1.00,
            scaleMobile: 1.00,
            color: 0x1a4c7a,
            backgroundColor: 0x030405,
            points: 13.00,
            maxDistance: 20.00,
            spacing: 18.00,
            showDots: true,
            opacity: 0.7
        });

        // Навбар скролл
        const nav = document.getElementById('nav');
        const backToTop = document.getElementById('backToTop');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 80) nav.classList.add('scrolled');
            else nav.classList.remove('scrolled');
            if (window.scrollY > 500) backToTop.classList.add('visible');
            else backToTop.classList.remove('visible');
        });

        // Гамбургер
        const hamburger = document.getElementById('hamburger');
        const mobileMenu = document.getElementById('mobileMenu');
        hamburger.addEventListener('click', () => {
            hamburger.classList.toggle('active');
            mobileMenu.classList.toggle('active');
        });
        window.closeMobile = function() {
            hamburger.classList.remove('active');
            mobileMenu.classList.remove('active');
        };

        // Кнопка наверх
        document.getElementById('backToTop').addEventListener('click', () => {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });

        // Анимации появления при скролле (NEXUS observer)
        const observerOptions = { threshold: 0.15, rootMargin: '0px 0px -50px 0px' };
        const animObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);
        document.querySelectorAll('.glass-card, .tariff-card, .gallery-item, .game-item, .faq-item, .stat-item').forEach(el => animObserver.observe(el));

        // Слайдер тарифов
        const track = document.getElementById('tariffsTrack');
        const prevBtn = document.getElementById('prevTariffBtn');
        const nextBtn = document.getElementById('nextTariffBtn');
        if (track && prevBtn && nextBtn) {
            const isMobile = () => window.innerWidth <= 768;
            let currentIndex = 0;
            let cardWidth = 0;
            const gap = 24;

            function updateCardWidth() {
                if (track.children.length) cardWidth = track.children[0].offsetWidth;
            }
            function getMaxIndex() {
                if (!track.children.length || cardWidth === 0) return 0;
                const wrapperWidth = track.parentElement.offsetWidth;
                const visible = Math.max(1, Math.floor((wrapperWidth + gap) / (cardWidth + gap)));
                return Math.max(0, track.children.length - visible);
            }
            function updateSlider() {
                if (isMobile()) {
                    track.style.transform = 'none';
                    prevBtn.style.display = 'none';
                    nextBtn.style.display = 'none';
                    return;
                }
                prevBtn.style.display = 'flex';
                nextBtn.style.display = 'flex';
                const maxIdx = getMaxIndex();
                if (currentIndex > maxIdx) currentIndex = maxIdx;
                track.style.transform = `translateX(-${currentIndex * (cardWidth + gap)}px)`;
            }
            prevBtn.addEventListener('click', () => {
                if (isMobile()) return;
                const maxIdx = getMaxIndex();
                currentIndex = currentIndex > 0 ? currentIndex - 1 : maxIdx;
                updateSlider();
            });
            nextBtn.addEventListener('click', () => {
                if (isMobile()) return;
                const maxIdx = getMaxIndex();
                currentIndex = currentIndex < maxIdx ? currentIndex + 1 : 0;
                updateSlider();
            });
            window.addEventListener('resize', () => {
                updateCardWidth();
                updateSlider();
            });
            setTimeout(() => {
                updateCardWidth();
                updateSlider();
            }, 100);
        }

        // FAQ аккордеон
        document.querySelectorAll('.faq-question').forEach(q => {
            q.addEventListener('click', function() {
                const item = this.closest('.faq-item');
                const isActive = item.classList.contains('active');
                document.querySelectorAll('.faq-item').forEach(i => i.classList.remove('active'));
                if (!isActive) item.classList.add('active');
            });
        });

        // Плавный скролл для якорей
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) target.scrollIntoView({ behavior: 'smooth', block: 'start' });
            });
        });

        // Эффект параллакса для hero
        window.addEventListener('scroll', () => {
            const hero = document.getElementById('hero');
            if (hero) {
                const scroll = window.scrollY;
                hero.style.transform = `translateY(${scroll * 0.2}px)`;
                hero.style.opacity = Math.max(0, 1 - scroll / 700);
            }
        });

        // Инициализация мобильного меню
        document.querySelectorAll('.mobile-menu a').forEach(link => {
            link.addEventListener('click', closeMobile);
        });
    })();
    </script>
</body>
</html>